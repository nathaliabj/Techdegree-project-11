import React from 'react';
import Form from './Form';
import Nav from './Navigation';

const Header = props =>{
  return (
    <header>
      <Form/>
      <Nav/>
    </header>
  );
}
export default Header;
